const TABLE_NAME_SESSIONS = 'postgress_sessions' // library for telegraf sessions wants this name
const TABLE_NAME_JOBS = 'jobs'
const TABLE_NAME_ENDPOINTS = 'endpoints'

module.exports = { TABLE_NAME_ENDPOINTS, TABLE_NAME_SESSIONS, TABLE_NAME_JOBS }